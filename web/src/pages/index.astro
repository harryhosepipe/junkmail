---
import Base from "../layouts/Base.astro";
import HomeFeed from "../features/images/components/HomeFeed.svelte";
import VoteModule from "../features/voting/components/VoteModule.svelte";

export const prerender = true;
---

<Base title="Junkmail" description="Public gallery of junkmail images with fast voting.">
  <section class="hero">
    <div class="headline">One man's trash is another man's treasure.</div>
    <p class="subtle">
      The best junkmail,...
    </p>
    <div class="muted-list">
      <span class="chip">Anonymous voting</span>
      <span class="chip">Invite-only uploads</span>
      <span class="chip">Bias-resistant rankings</span>
    </div>
  </section>

  <section class="panel" style="margin-top: 28px;">
    <div class="headline" style="font-size: 22px;">Pick one</div>
    <div style="margin-top: 16px;">
      <VoteModule client:load />
    </div>
  </section>

  <section class="panel" style="margin-top: 24px;">
    <div class="headline" style="font-size: 22px;">Fresh + Top mix</div>
    <HomeFeed client:load />
  </section>
</Base>
